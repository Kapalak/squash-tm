# squash-tm

Squash is a testing tool.

With this repos you will be able to test it locally :

In One step

`git clone https://github.com/Kapalak/squash-tm.git `

`cd squash-tm`

`docker-compose up -d`

Navigate to this URL 

`http://localhost:8090/squash/`

set admin/admin
